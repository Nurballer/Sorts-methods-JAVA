public class Practice(args) {
public static void
int[l arr -
mergeSort(arr) ;
for(int = O;
< art'. length; 1++)
        System.ovt.print(arrßl +
        private static void arr) {
    int n arr.tength;
    if(n I) return;
    int mid = n / 2
    into 1 -
            - new int[midl;
    int[] r
            = new int[n - mid];
    for(int = 0; i < mid; i++)
= arr[i];
    for(int = mid; i < n; i")
            - midl - arr[il;
    123,
            55,
            3,
            1000};
mergeSort(t) ;
mergeSort(r) ;
merge (arr. I,
       r);